{
  "name": "read-later-random-extension",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build && npm run copy-files",
    "dev": "vite build --watch",
    "copy-files": "cp manifest.json dist/ && cp popup.html dist/"
  },
  "dependencies": {
    "firebase": "^12.4.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.16",
    "tailwindcss": "^4.1.16",
    "vite": "^7.1.12"
  }
}
